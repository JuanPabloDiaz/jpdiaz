---
interface Props {
	text: string;
	href: string;
}

const { text, href } = Astro.props;
---

<a href={href} class="cursor:pointer text-gray-900 text-sm font-semibold leading-6 dark:text-white"
	>{text}</a
>
